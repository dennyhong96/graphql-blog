import { createMuiTheme } from "@material-ui/core";

export default createMuiTheme({
  typography: {
    fontFamily: "'Rubik', sans-serif",
  },
  palette: {
    primary: {
      main: "#3d0e5b",
    },
    secondary: {
      main: "#d11692",
    },
    background: {
      default: "#fcfcfc",
    },
  },
});
